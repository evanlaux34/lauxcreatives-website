Hairline rule that divides sections — plain, or with a centered Courier label flanked by two lines.

```jsx
<Divider />
<Divider label="Chicago · 2024" />
<Divider label="The Work" tone="onDark" />
```

Tones: `paper`, `onDark`.
