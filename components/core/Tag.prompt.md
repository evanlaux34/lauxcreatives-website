Small uppercase Courier label chip for categories and gallery filters.

```jsx
<Tag>Weddings</Tag>
<Tag variant="solid">Featured</Tag>
<Tag variant="accent">Engagements</Tag>
<Tag variant="pill">Family</Tag>
```

Variants: `outline` (default), `solid` (olive), `accent` (gold outline), `pill`. Square unless `pill`.
