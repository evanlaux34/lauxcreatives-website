Underline-style editorial form field — a Courier uppercase label over a Garamond input on a hairline that warms to gold on focus.

```jsx
<Input label="Your name" placeholder="Evan & Alexis" />
<Input label="Tell us about your day" multiline />
```

Set `multiline` for a textarea. No box — just the baseline rule, matching the brand's restrained, paper-first feel.
